# Agent entrypoint

Read and follow `CLAUDE.md`, then read `HANDOFF.md` before acting. This file is intentionally only a portable pointer so cloud backup and Windows restore do not depend on symlink support or duplicate project rules.
